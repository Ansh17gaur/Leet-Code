class Solution(object):
    def intersection(self, nums1, nums2):
        seen = {}
        result = []
        for num in nums1:
            seen[num] = True
        for num in nums2:
            if num in seen and num not in result:
                result.append(num)
        return result
